{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ScrapeFrontDocsInput",
  "type": "object",
  "properties": {
    "model": { "type": "string", "description": "LLM model to use." },
    "provider": { "type": "string", "enum": ["openai", "anthropic", "google", "deepseek", "grok"], "description": "LLM provider to use." }
  },
  "required": ["model", "provider"],
  "additionalProperties": false
} 