[project]
name = "rcrainfo-account-violations"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "duckdb",
    "pandas",
    "python-dotenv",
    "logger",
    "numpy"
]
